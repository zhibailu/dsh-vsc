# Screenshots — 当前截图清单

README 用这些截图展示功能。**高瘦图不要横着排**——规范做法：每张图独立成行、居中、按 `width` 缩放。

当前 7 张已就位（文件名与 README 引用一致）：

| 文件 | 内容 | 尺寸（宽×高） | 在 README 中 width |
|---|---|---|---|
| `sidebar.png` | 侧边栏 + 对话（合并一张：会话列表、流式回复、⚙ 动作、计时、+N-M） | 549×1312 | 290 |
| `ask-menu.png` | 编辑器选中代码 → 右键菜单「DSH: Ask about selection」 | 887×800 | 380 |
| `ask-card.png` | 面板里弹出的结构化 Ask 卡片（文件/选区/工作区/分支） | 566×1328 | 290 |
| `approval.png` | 审批卡片（允许一次 / 拒绝） | 683×301 | 460 |
| `question.png` | 选择题卡片（单选/多选、推荐徽章、自定义回答） | 566×1307 | 290 |
| `review.png` | Review Agent Changes：改动文件 → git diff | 557×253 | 440 |
| `mode-picker.png` | 新建会话的模式下拉（标准/PTC/极简/创造） | 656×215 | 400 |

## 想重拍或新增

- 重拍：直接覆盖同名文件即可，README 自动生效（不需要改 README）。
- 新增：把文件放本目录，然后在 README 截图区补一块 `<p align="center"><img .../></p>`。
- 规范：图源清晰（≥ 2× 显示宽度）；`width` 只调宽度、高度自动；竖长图居中单独成行；别带真实 API key / 路径 / 隐私内容。
