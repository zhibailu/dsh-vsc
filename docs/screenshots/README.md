# docs/screenshots — README 截图占位

| 项 | 说明 |
|---|---|
| **谁可见** | 公开（推送到 GitHub，README 引用） |
| **为谁负责** | 维护者手动放置截图 |
| **行为边界** | 只放 README 引用的三张截图，文件名必须精确匹配 |

## 需要的文件（傻瓜式操作：照拍 → 放进此目录 → 文件名一致即生效）

| 文件名 | 拍摄步骤 |
|---|---|
| `sidebar.png` | 打开 VS Code → 左侧活动栏点 DSH 图标 → 截整个侧边栏（含会话列表和输入框） |
| `chat.png` | 侧边栏发一条消息让 agent 干活 → 等有工具调用 → 截屏（含「⚙ 动作」折叠块、思考栏、计时器、+N-M） |
| `ask-menu.png` | 编辑器选中一段代码 → 右键 → 截屏（含 "Ask DSH" 菜单项） |

## 注意

- README 用相对路径 `docs/screenshots/xxx.png` 引用，文件放对即生效
- 不用改 README，不用处理尺寸，随便截
